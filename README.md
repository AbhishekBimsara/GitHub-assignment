# GitHub-assignment

## Purpose
This repository was created as part of an academic assignment to demonstrate basic GitHub usage, including creating a repository, uploading files, and documenting a project.

## Python Script does
The `hello.py` Python script prints a simple greeting message to the console. It serves as an introductory example of a Python program and verifies that the repository is set up correctly.
